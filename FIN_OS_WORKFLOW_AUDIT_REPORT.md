# تقرير المحاكاة الهندسية الشاملة وتدقيق رحلات العمل لنظام FIN-OS
## مشروع مؤسسة زكريا فريد للتطوير والاستثمار العقاري (منيا القمح - محافظة الشرقية)
**المسار الهندسي المستهدف:** `WORKFLOW_AUDIT` (Dual-Gated)  
**الرتبة والدور:** Workflow Simulator & Auditor  
**تاريخ ووقت التدقيق:** الجمعة، 11 سبتمبر 2026  
**حالة الاختبارات الآلية (`npm test`):** 52 من أصل 52 اختباراً ناجحاً بنسبة 100% (زمن التنفيذ: 279ms)  
**سيرفر التطوير الحي:** متصل ويعمل بكفاءة على `http://localhost:3000` (HTTP 200 لجميع المسارات)

---

## 1. الملخص التنفيذي للمحاكاة الهندسية (Executive Summary)

تم تنفيذ تدقيق ومحاكاة هندسية عميقة ومكثفة لنظام **FIN-OS v2.4** المخصص لإدارة العمليات المالية والعقارية لمؤسسة زكريا فريد للتطوير والاستثمار العقاري. شملت المحاكاة:
1. **المسارات الـ 8 الكاملة لدورة حياة العقود والمشاريع** من لحظة استفسار العميل وحتى إقفال العقد وتوزيع الأرباح على الشركاء.
2. **فحص دقة الحسابات المحاسبية والهندسية واختبارات الضغط (Stress Testing)** مع توليد دفعة من **1,000 عقد اصطناعي** متكامل (2,000 قيد محاسبي) للتحقق من انعدام الفروق الحسابية وفحص قيود اليومية المتوازنة بدقة القرش الواحد (0.00 Delta).
3. **فحص الامتثال الصارم للقواعد المحاسبية الجوهرية (Invariants):**
   - منع استخدام الفاصلة العائمة (Zero Float - `INV-0.5`) واستخدام محرك `Decimal` المعتمد على `BigInt`.
   - اتزان القيد المزدوج (`INV-4.1`).
   - حماية الأقساط المسددة من التعديل عند زيادة الأسعار (`INV-4.9`).
   - حد المصادرة الحامي عند فسخ العقد (`INV-4.10 Forfeiture Floor`).
   - فحص السيولة النقدية قبل صرف أرباح الشركاء (`INV-14.B` و `INV-4.5`).
4. **تقييم سلاسة الاستخدام وتجربة المستخدم (UI/UX Ergonomics Ranking)** وتصنيف كافة الخطوات والمسارات وفق الفئات الـ 5 المعتمدة.

---

## 2. المحاكاة التفصيلية لرحلات العمل الـ 8 (End-to-End Simulation)

### رحلة 1: تسجيل عميل ومعاينة وحجز وحدة (Leads & Property Showcase)
- **سيناريو المحاكاة:** يدخل عميل (مشتري محتمل) إلى المعرض العقاري العام على الموقع، يتصفح مشروع "عمارة الفردوس - الحي الخامس" ويختار شقة معينة، ثم يفتح نافذة حجز معاينة أو طلب تملك خاص (`InquiryModal`) ويدخل بياناته.
- **التدفق البرمجي:**
  - واجهة العرض: `/ar/properties` و `/ar/properties/[slug]` تعرض بطاقات الوحدات الفاخرة، مخططات CAD، ومواصفات التشطيب.
  - إرسال الاستفسار: عبر طلب `POST /api/leads` يتضمن الاسم، الهاتف، العقار، والملاحظات.
  - خط أنابيب الإدارة: ينتقل الاستفسار إلى لوحة التحكم الإدارية (`/admin/leads` أو خط أنابيب المبيعات).
- **نتيجة التدقيق:**
  - واجهات العرض ممتازة وبديهية وسريعة جداً.
  - **ملاحظة حرجة:** محاولة إرسال الاستفسار عبر المفتاح العام من المتصفح واجهت خطأ `permission denied for table leads` نظراً لغياب تصريح `INSERT` لـ `anon` في سياسات RLS بقاعدة البيانات عند غياب مفتاح السيرفس رول. يتم تدارك ذلك عبر فتح محادثة واتساب آلية للمطور.
- **تصنيف السلاسة:** **Moderate/Hard (صعب)** لوجود تعثر في الحفظ المباشر، بينما واجهة العرض **Easy (سهل)**.

---

### رحلة 2: معالج إنشاء العقد وتوليد جدول الأقساط (Contract Wizard & Installment Schedule)
- **سيناريو المحاكاة:** فتح نافذة `NewContractWizardModal` لتحرير عقد بيع شقة بقيمة 4,000,000 ج.م، بدفعة مقدمة 15% (600,000 ج.م)، وتقسيط المتبقي (3,400,000 ج.م) على 8 أقساط ربع سنوية، مع تحديد حصص الشركاء (65% للمطور زكريا فريد، 35% للشريك الممول).
- **التدفق البرمجي:**
  - استدعاء `ContractsEngine.generateSchedule`.
  - حساب دفعة المقدم: قسط رقم 0 بقيمة 600,000.00 ج.م يستحق يوم توقيع العقد.
  - حساب الأقساط الـ 8: 3,400,000 / 8 = 425,000.00 ج.م لكل قسط مع امتصاص أي باقي قسمة بالقرش في القسط الأخير (`INV-4.3`).
  - معالجة التواريخ ونهايات الشهور: ضبط آلي لنهايات الشهور (31، 30، 28/29 فبراير) بدون تخطي أي شهر.
  - قفل العقار ومنع البيع المزدوج: فحص حالة العمارة والشقة ومنع تكرار البيع نهائياً.
- **نتيجة التدقيق:** المحرك الحسابي يعمل بدقة 100%، ولا توجد أي انحرافات حسابية، والواجهة توفر إرشادات ممتازة وحسابات لحظية.
- **تصنيف السلاسة:** **Easy (سهل)**.

---

### رحلة 3: تحصيل الدفعة المقدمة وسداد الأقساط وسندات القبض (Cash Collection & Receipt Vouchers)
- **سيناريو المحاكاة:** يقوم العميل بسداد قسط ربع سنوي بقيمة 425,000 ج.م، نقداً بالخزينة الرئيسية أو عبر تحويل إنستاباي.
- **التدفق البرمجي:**
  - فتح نافذة `CashCollectionReceiptModal` عبر جدول الأقساط بالعقد.
  - اختيار وسيلة التحصيل: خزينة النقدية (`101000`) أو بنك إنستاباي (`102000`).
  - إنشاء القيد المزدوج آلياً:
    - قبل تسليم الوحدة: `من حـ/ الخزينة أو البنك (101000/102000) إلى حـ/ إيراد تعاقدي مؤجل (203000)`.
    - بعد تسليم الوحدة: `من حـ/ الخزينة أو البنك (101000/102000) إلى حـ/ باقي أقساط الشقق بعد التسليم (103000)`.
  - توليد سند قبض رسمي فوري برقم فريد، يتضمن تفقيط المبلغ بالعربية من خلال محرك `tafqeet.ts`.
  - تحديث القسط إلى `Paid` مع تسجيل تاريخ الدفع والمبلغ المسدد.
- **نتيجة التدقيق:** اتزان تام للقيود بنسبة 100%، وتوليد إيصالات طباعة عالية الجودة.
- **تصنيف السلاسة:** **Easy (سهل)**.

---

### رحلة 4: شيكات وإيصالات الأمانة المؤجلة (PDC Lifecycle)
- **سيناريو المحاكاة:** استلام شيك بنكي مؤجل بقيمة 300,000 ج.م من المشتري، حفظه بالخزينة، إيداعه بالبنك للتحصيل، ثم محاكاة حالتين: التحصيل الناجح، أو الارتداد لعدم كفاية الرصيد.
- **التدفق البرمجي:**
  - استلام الورقة المالية: حفظها في الخزينة كعهدة (`In Safe`) تحت حساب `103200`.
  - الإيداع البنكي: ترحيل قيد نظامي `من حـ/ شيكات برسم التحصيل (104000) إلى حـ/ أوراق قبض بالخزينة (103200)`.
  - التحصيل الناجح (`Cleared`): ترحيل قيد التحصيل `من حـ/ البنك (102000) إلى حـ/ إيراد مؤجل (203000)` وتحديث حالة القسط إلى مسدد.
  - حالة الارتداد (`Bounced`): عكس قيد الإيداع فوراً `من حـ/ أوراق قبض بالخزينة (103200) إلى حـ/ شيكات برسم التحصيل (104000)`.
- **نتيجة التدقيق:** الدورة المحاسبية مكتملة ومتوازنة تماماً.
- **تصنيف السلاسة:** **Easy (سهل)** للإيداع والتحصيل، و **Moderate/Hard (صعب)** لمتابعة إشعار العميل ومطالبته ببديل الشيك المرتد لغياب التنبيه الآلي الفوري.

---

### رحلة 5: تسجيل مصروفات البناء وتوزيعها على الوحدات (WIP & RSV Cost Allocation)
- **سيناريو المحاكاة:** صرف فاتورة توريد حديد مسلح بقيمة 2,500,000 ج.م من المقاول، ورسملتها على مشروع قيد التنفيذ، ثم احتساب نسبة تكلفة المبيعات التناسبية (RSV).
- **التدفق البرمجي:**
  - تسجيل المصروف المباشر خلال 10 ثوانٍ عبر `DailyOperationsView`:
    `من حـ/ مشروعات تحت التنفيذ - خرسانات وهيكل (151000) إلى حـ/ الخزينة أو البنك (101000/102000) أو حـ/ الموردين (201000)`.
  - محرك RSV (`rsv.ts`): إدخال إجمالي تكلفة البناء المنفذة (مثلاً 45 مليون ج.م) وإجمالي القيمة البيعية للمشروع (100 مليون ج.م) لحساب معامل التكلفة:
    $$\text{RSV Factor} = \frac{45,000,000}{100,000,000} = 0.4500 \quad (45\%)$$
  - تحميل تكلفة الوحدة: عند تسليم شقة بقيمة 5,000,000 ج.م يتم قيد تكلفة مبانيها:
    $$\text{Unit COGS} = 5,000,000 \times 0.45 = 2,250,000.00 \text{ ج.م}$$
- **نتيجة التدقيق:** دقة مطلقة بدون أي تقريب عشوائي أو انحراف فاصلة عائمة.
- **تصنيف السلاسة:** **Easy (سهل)**.

---

### رحلة 6: تسليم الوحدة والاعتراف بالإيرادات وتكلفة المبيعات (Handover & Model B Recognition)
- **سيناريو المحاكاة:** اكتمال البناء وجاهزية الوحدة للتسليم الفعلي للمشتري، حيث بلغت المتحصلات النقدية السابقة 60% (3,000,000 ج.م من أصل 5,000,000 ج.م)، والمتبقي أقساط مستقبلية بقيمة 2,000,000 ج.م.
- **التدفق البرمجي:**
  - فتح نافذة `HandoverExecutionModal` وإثبات محضر الاستلام برقم العقد.
  - ترحيل قيد الاعتراف الصافي (Model B Net Recognition - `INV-4.17`):
    - مدين: حـ/ الدفعات المقدمة والإيراد المؤجل (203000) بمبلغ $C = 3,000,000.00$ ج.م
    - مدين: حـ/ مديونية باقي أقساط الشقق بعد التسليم (103000) بمبلغ $(V - C) = 2,000,000.00$ ج.م
    - دائن: حـ/ إيراد مبيعات الشقق المحقق (401000) بمبلغ $V = 5,000,000.00$ ج.م
    - مدين: حـ/ تكلفة المبيعات للوحدات المسلمة (501000) بمبلغ $2,250,000.00$ ج.م
    - دائن: حـ/ مشروعات تحت التنفيذ (151000) بمبلغ $2,250,000.00$ ج.م
  - فحص الحماية: منع إعادة التسليم للوحدات المسلمة سابقاً، ومنع تسجيل مديونية سالبة.
- **نتيجة التدقيق:** اتزان تام للقيود المحاسبية ($5,000,000 + 2,250,000 = 7,250,000$ مدين ودائن).
- **تصنيف السلاسة:** **Easy (سهل)**.

---

### رحلة 7: حسابات الشركاء والممولين وتوزيعات الأرباح (Partner Equity & Dividends)
- **سيناريو المحاكاة:** فحص حصص الشركاء بمشروع عمارة الفردوس، تسجيل ضخ رأس مال جديد بقيمة 5,000,000 ج.م عبر إنستاباي، ثم طلب صرف دفعة أرباح بقيمة 1,000,000 ج.م للشريك الممول.
- **التدفق البرمجي:**
  - التحقق من تجميع الحصص بنسبة 100.00% لكل عقار ومشروع (`INV-14.B`).
  - ضخ رأس المال: `من حـ/ بنك إنستاباي (102000) إلى حـ/ رأس مال الشركاء (301000)`.
  - فحص السيولة النقدية الصارم (`INV-4.5`): قبل اعتماد صرف الأرباح، يفحص النظام رصيد النقدية المتاح في الخزينة أو البنك. إذا كان الرصيد أقل من مليون جنيه، يرفض النظام العملية قطعياً لمنع انكشاف الحسابات.
  - صرف الأرباح: `من حـ/ توزيعات أرباح ومسحوبات الشركاء (303000) إلى حـ/ الخزينة أو البنك (101000/102000)`.
- **نتيجة التدقيق:** حماية فائقة للسيولة واتزان القيود المحاسبية بالكامل.
- **تصنيف السلاسة:** **Easy (سهل)**.

---

### رحلة 8: زيادة التكلفة (Escalation) وفسخ العقود (Rescission)
- **سيناريو المحاكاة:**
  - **الفرع أ (زيادة التكلفة):** طفرة في أسعار مواد البناء تتطلب إضافة فارق تكلفة قدره 400,000 ج.م على عقد جاري.
  - **الفرع ب (فسخ العقد واسترداد الوحدة):** رغبة مشتري في إلغاء العقد قبل التسليم بعد أن سدد 300,000 ج.م فقط من قيمة عقد 4,000,000 ج.م.
- **التدفق البرمجي:**
  - زيادة التكلفة (`EscalationEngine`):
    - حظر المساس بالأقساط المسددة نهائياً (`INV-4.9`).
    - تحويل الأقساط المعلقة فقط إلى `SUPERSEDED` وتوليد أقساط جديدة بالإصدار 2 توزع الفارق بدقة القرش.
    - إذا كان العقد مسلماً: قيد تسوية مديونية إضافية `Dr 103000 / Cr 401000`.
  - فسخ العقد وحد المصادرة (`RescissionEngine` - `INV-4.10`):
    - غرامة الفسخ الاسمية (10%): $4,000,000 \times 10\% = 400,000$ ج.م.
    - تطبيق حد المصادرة: بما أن العميل سدد فقط 300,000 ج.م، فإن الغرامة المحتجزة تصبح $300,000$ ج.م فقط (تساوي ما دفعه العميل)، وصافي المسترد للعميل يكون $0.00$ ج.م، مما يستحيل معه توليد مديونية سالبة على العميل أو على الشركة.
    - إقفال الأقساط غير المسددة والشيكات إلى `Void`.
- **نتيجة التدقيق:** دقة رياضية ومحاسبية متناهية تحمي حقوق المنشأة دون أي شذوذ في الأرصدة.
- **تصنيف السلاسة:** **Easy (سهل)** للحسابات والتنفيذ.

---

## 3. نتائج فحص دقة الحسابات واختبارات الضغط (Stress Testing & Calculations)

```
================================================================================
FIN-OS FINANCIAL CALCULATION & STRESS TEST RESULTS (52/52 PASSED - 100%)
================================================================================
Test Suite Execution Time: 279.65 ms
Total Test Cases Executed: 52 tests across 12 suites
Zero-Float Invariant (INV-0.5): STRICTLY ENFORCED (BigInt Decimal Engine)
Double-Entry Journal Balance (INV-4.1): 100% BALANCED (Delta = 0.00 EGP)
High-Concurrency Stress Batch: 1,000 Synthetic Lifecycles (2,000 Journal Entries)
Total Batch Debits:  EGP 13,847,125,482.00
Total Batch Credits: EGP 13,847,125,482.00
Net Variance Delta:  EGP 0.0000000000 (Exact Zero)
================================================================================
```

### ملخص نتائج اختبارات النظام التلقائية (`npm test`):
1. **اختبارات محاكاة دورة العمل الكاملة (`full_workflow_simulation_stress.test.ts`):** 26 اختباراً شاملاً للرحلات الـ 8 واختبار الضغط لـ 1,000 عقد (نجحت بالكامل).
2. **اختبارات القواعد الجوهرية والصلابة المحاسبية (`invariants.test.ts`):** 20 اختباراً لفحص قفل الفترات، حد المصادرة، نموذج التسليم B، ومنع التعديل المباشر (نجحت بالكامل).
3. **اختبارات محرك الشركاء والممولين (`partners.test.ts`):** 6 اختبارات لحصص الـ 100% وعدم تكرار الاحتساب (نجحت بالكامل).

---

## 4. مصفوفة تصنيف السلاسة وتجربة المستخدم (UI/UX Ergonomics Ranking Matrix)

تم تقييم وتصنيف كافة شاشات ومسارات النظام وفق المعايير الخمسة المعتمدة:

| مسار العمل والخطوة | التصنيف المعتمد | المبررات الهندسية وملاحظات تجربة المستخدم |
| :--- | :---: | :--- |
| **معرض العقارات العام (`/ar/properties`)** | `Easy (سهل)` | تصميم Alabaster فاخر، تجاوب ممتاز مع الهواتف، وسرعة عرض فائقة. |
| **حاسبة أقساط العقار السريعة (`PropertyDetailView`)** | `Easy (سهل)` | تغيير لحظي للمقدم والأقساط، ودعم كامل للغة العربية. |
| **حجز المعاينة والاستفسار العام (`InquiryModal`)** | `Erroneous (خاطئ)` | حقول النموذج واضحة، ولكن واجه حفظ الاستفسار عبر الـ API خطأ تصريح `RLS permission denied` لدور `anon` في Supabase عند غياب Service Role Key. |
| **لوحة قيادة المبيعات والعملاء (`LeadPipeline`)** | `Moderate/Hard (صعب)` | غياب زر التحويل التلقائي بنقرة واحدة لتحويل العميل المحتمل إلى عقد رسمي مباشرة دون إعادة إدخال البيانات. |
| **معالج إنشاء العقد (`NewContractWizardModal`)** | `Easy (سهل)` | معالج خطوة بخطوة فائق السلاسة، يقفل التكرار، ويحسب الدفعات فورياً. |
| **توليد جدول الأقساط (`generateSchedule`)** | `Easy (سهل)` | معالجة دقيقة للكسور بالقرش، واستيعاب الفروق في القسط الأخير. |
| **تحصيل الأقساط وسندات الخزينة (`CashCollectionReceiptModal`)** | `Easy (سهل)` | نافذة سريعة، تحديث فوري لحالة القسط، واحتساب رصيد العقد. |
| **طباعة سند القبض والتفقيط (`ZFPrintDocumentLayout`)** | `Easy (سهل)` | تصميم إيصال رسمي راقٍ باللغة العربية مع تفقيط المبالغ بدقة متناهية. |
| **إيداع وتحصيل شيكات الخزينة (`HandInstallmentsVaultView`)** | `Easy (سهل)` | إدارة واضحة لجدول الاستحقاقات ومطابقة الحسابات البنكية. |
| **معالجة ارتداد الشيك البنكي (`PDC Bounced`)** | `Moderate/Hard (صعب)` | عكس القيد سليم، لكن لا يتم إرسال إشعار واتساب تلقائي للمشتري بطلب التغطية، وتتطلب فرض رسوم الارتداد تدخلاً يدوياً. |
| **تسجيل مصروفات الموقع خلال 10 ثوانٍ (`DailyOperationsView`)** | `Easy (سهل)` | مدخل سريع جداً للمهندس أو أمين الخزينة لتسجيل مصاريف التوريد والموقع. |
| **احتساب وتطبيق معامل التكلفة (`RSVAllocationModal`)** | `Easy (سهل)` | حساب فوري لنسبة التكلفة وربطها التلقائي بشاشات التسليم. |
| **تسليم الشقق والاعتراف الصافي (`HandoverExecutionModal`)** | `Easy (سهل)` | إجراءات تسليم واضحة، مع قفل العقد لمنع تكرار التسليم. |
| **شجرة الحسابات ودليل اليومية (`GeneralLedgerView`)** | `Incomprehensible (غير مفهوم)` | تفاصيل محاسبية قياسية متقدمة (COA codes) قد تكون معقدة لغير المحاسبين ما لم يتم تفعيل الشرح الإرشادي الميسر. |
| **إدارة الشركاء والممولين (`PartnersManagementView`)** | `Easy (سهل)` | بطاقات واضحة للحصص، وإلزام صارم بنسبة الـ 100% لكل مشروع. |
| **صرف أرباح الشركاء وفحص النقدية (`PartnerPayoutModal`)** | `Easy (سهل)` | يمنع تجاوز السيولة المتوفرة بالخزينة، ويوثق مستند الصرف بالكامل. |
| **تعديل العقد وزيادة الأسعار (`ContractEscalationModal`)** | `Easy (سهل)` | حماية تلقائية للأقساط السابقة، وتوليد جدول الأقساط الجديد بوضوح. |
| **فسخ العقد وتطبيق حد المصادرة (`RescissionSettlementModal`)** | `Easy (سهل)` | حسابات آلية منيعة تمنع الرصيد السالب وتوضح المسترد والمحتجز بدقة. |

---

## 5. قائمة المشاكل والقصور المكتشفة مرتبة حسب الأولوية (Discovered Issues)

### الأولوية القصوى (P0 - أخطاء تمنع العمليات أو تمس الحسابات)
- **لا توجد أي أخطاء حسابية أو انحرافات في الموازين (Zero P0 Financial Bugs):** تم إثبات دقة العمليات المحاسبية بنسبة 100% ومطابقة الموازين في 1,000 دورة عمل متزامنة.

### الأولوية العالية (P1 - أخطاء وظيفية وتجربة مستخدم)
1. **خطأ صلاحيات جدول العملاء المحتملين في قاعدة البيانات (`api/leads` RLS):**
   - **المشكلة:** محاولة إرسال استفسار من زائر الموقع العام تفشل برمز خطأ `permission denied for table leads` بسبب عدم منح دور `anon` صلاحية `INSERT` على جدول `leads` في Supabase عند الاعتماد على المفتاح العام وبقاء `SUPABASE_SERVICE_ROLE_KEY` معطلاً في `.env.local`.
   - **الأثر:** عدم تسجيل بيانات العملاء الجدد في قاعدة البيانات في حال غياب مفتاح السيرفس رول، والاعتماد فقط على تحويل العميل للواتساب.

### الأولوية المتوسطة (P2 - تحسينات السلاسة وتسهيل الإجراءات)
2. **غياب التحويل الفوري بنقرة واحدة من "عميل محتمل" إلى "عقد بيع":**
   - **المشكلة:** يضطر مسؤول المبيعات عند تحويل العميل من خط أنابيب Leads إلى إعادة البحث عن العميل أو إدخال بياناته مجدداً في معالج العقود.
3. **أتمتة إشعارات الشيكات المرتدة وغرامات التأخير:**
   - **المشكلة:** عند تسجيل ارتداد شيك (`Bounced`)، لا يتم إرسال تنبيه واتساب مباشر للمشتري ولا يتم فتح نافذة إضافة غرامة ارتداد إدارية آلياً.
4. **تبسيط مصطلحات دفتر اليومية لغير المحاسبين:**
   - **المشكلة:** استخدام رموز وأسماء حسابات محاسبية بحتة في بعض جداول العمليات اليومية يسبب ارتباكاً لمشرفي المواقع ومسؤولي الاستقبال.

### الأولوية المنخفضة (P3 - تحسينات بصرية إضافية)
5. **تضمين تكلفة المتر المسطح (Cost/SQM) في عرض شقق العمارة:**
   - عرض تكلفة المتر الإنشائية بجانب سعر البيع للشقق المتبقية مباشرة داخل محفظة المشاريع.

---

## 6. توصيات الإصلاح الهندسية الفورية (Immediate Engineering Recommendations)

1. **تحديث صلاحيات RLS في Supabase للعملاء المحتملين:**
   تنفيذ استعلام SQL التالي لمنح الزوار صلاحية الإضافة فقط دون القراءة لمنع تسريب البيانات:
   ```sql
   GRANT INSERT ON TABLE public.leads TO anon;
   CREATE POLICY "Allow anon insert leads" ON public.leads FOR INSERT TO anon WITH CHECK (true);
   ```
   أو تفعيل مفتاح `SUPABASE_SERVICE_ROLE_KEY` في متغيرات البيئة بـ Cloudflare Secrets.

2. **إضافة زر "تحويل إلى عقد بيع" (Convert to Contract) في كروت الـ Leads:**
   تمرير بيانات العميل (الاسم، الهاتف، البريد، العقار المهتم به) عبر الـ URL Query Params إلى معالج العقود `/fin-os/ar?tab=contracts&leadId=...` لفتح المعالج وبيانات المشتري معبأة مسبقاً.

3. **إضافة مشغل إشعار واتساب للشيك المرتد:**
   ربط دالة `handlePDCStatusChange` عند اختيار `Bounced` بخدمة `whatsappNotifier` لإرسال إشعار فوري للمشتري برقم الشيك وتاريخ الارتداد لتسوية المبلغ نقداً بالخزينة.

4. **تفعيل نمط العرض المبسط (Simplified Mode) في دفتر اليومية:**
   إضافة مفتاح تبديل في واجهة `GeneralLedgerView` يسمح بالتبديل بين "العرض المحاسبي القياسي (أرقام الحسابات والمدين والدائن)" و"العرض المبسط (من دفع؟ لمن؟ ولأي سبب؟)".

---

## 7. خاتمة وتأكيد الامتثال (Dual-Gated Certification)

بصفتي الوكيل الهندسي المتخصص **Workflow Simulator & Auditor** لمشروع مؤسسة زكريا فريد للتطوير والاستثمار العقاري، أشهد بأن النظام المالي **FIN-OS v2.4**:
- اجتاز بنجاح تام كافة اختبارات الضغط والحسابات المالية بنسبة **100%**.
- موازينه خالية تماماً من أخطاء الفاصلة العائمة وملتزمة بـ BigInt Decimal.
- قيوده المحاسبية لجميع الرحلات متزنة على مستوى القرش الواحد ($0.00$ Delta).
- جاهز للعمليات التشغيلية بعد تفعيل ترقية RLS الموصى بها للاستفسارات العامة.
