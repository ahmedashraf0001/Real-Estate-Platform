{
  "scripts": {
    "build": "cd zakaria-farid && npm install && npx @opennextjs/cloudflare build"
  },
  "dependencies": {
    "@supabase/ssr": "^0.12.4",
    "@supabase/supabase-js": "^2.112.0"
  }
}
